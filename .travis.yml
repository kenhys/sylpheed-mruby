language: c
compiler:
  - gcc
  - clang
notifications:
  recipients:
    - kenhys@gmail.com
branches:
  only:
    - master
    - develop
env:
  - USE_DEBUG=yes
  - USE_DEBUG=no
before_script:
  - curl --location https://raw.github.com/kenhys/sylpheed-plugin-factory/master/misc/setup-travis.sh | sh
  - git submodule update --init --recursive
  - ./autogen.sh
script:
  - curl --location https://raw.github.com/kenhys/sylpheed-plugin-factory/master/misc/configure.sh | sh
  - make

